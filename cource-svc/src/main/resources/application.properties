spring.application.name=course-svc
server.port=8082

spring.datasource.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1
spring.datasource.username=system
spring.datasource.password=root
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Shared JWT secret (must match aut-svc) so tokens issued by aut-svc are valid here.
jwt.secret=5f2c8b9e1a4d6f70c3b8e2a9d4f1c6b7e0a3d5f8c2b9e4a7d0f3c6b1e8a5d2f9
jwt.expiration=86400000

# ---- MinIO object storage (videos + images) ----
minio.url=http://localhost:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket=courseconnect

# ---- Stripe payments ----
stripe.api-key=sk_test_placeholder
stripe.webhook-secret=whsec_placeholder
stripe.currency=usd
app.frontend-base=http://localhost:5500

app.cors.allowed-origins=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000,http://127.0.0.1:3000
